# Copy to .env and fill in. The package loads this via config.load_env().
# Anthropic key: synthesizes the dataset (generate/control) and judges actions.
ANTHROPIC_API_KEY=
# Hugging Face token (only if a chosen base model's repo is gated).
HF_TOKEN=
